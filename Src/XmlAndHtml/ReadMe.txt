XML and HTML

This PowerToy demonstrates a pair of context actions which add a missing "id" attribute
to XML and HTML tags respectively. The ReSharper features covered by this plug-in are:

 * Context Action

This plug-in illustrates the differences in handling XML and HTML inputs when processing
tags and modifying them. Though the code may appear similar, the plug-in shows that there
are fundamental API differences between the two approaches.